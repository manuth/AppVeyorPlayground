configuration:
  - Native
  - WSL

image: Visual Studio 2019

init:
  - echo "init all"

for:
-
  matrix:
    only:
      - configuration: Native
  init:
    - echo "init native"
-
  matrix:
    only:
      - configuration: WSL
  init:
    - echo "init wsl"
